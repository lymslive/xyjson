---
description: "核心代码开发 TODO 需求"
---

任务 ID:
!date +"%Y%m%d-%H%M%S"

需求 ID:
**$1**

需求内容（可用脚本从 `task_todo.md` 读取如下）：
=========================
!perl script/todo.pl **$1**
=========================

请仔细分析如上需求内容，大致工作流：
- 制定更详细的实施计划
  * 如果任务较复杂，请先将实施计划写入 `doing_plan.tmp/` 子目录，确认后再开始
- 按计划开始实施
  * 开发任务要确保编译与测试通过
  * 如果陷入 debug 循环 5 次以上仍未能解决问题，请放弃，视为任务失败
- 如果任务成功完成
  * 以任务 ID 为标题在 `tast_log.md` 末尾记录日志，参考已有格式
  * 将任务 ID 写回 `task_todo.md` 关联需求 ID
    + 可用更新脚本： perl script/todo.pl "需求ID" "任务ID"
  * 提交 git ，提交消息的 scope 为需求ID(**$1**)
    + 不要提交写在临时目录的计划文档，以及其他为调试所写的临时文件
- 如果任务失败，也在 `task_log.md` 记录日志，但不要后续提交

提醒：请不要读 `task_log.md` 与 `task_todo.md` 全文，可能比较大。
`task_todo.md` 用 perl 脚本读取一节。`task_log.md` 读最后 100 行左右参考格式就
行，当前工作日志追加在 `task_log.md` 末尾。
